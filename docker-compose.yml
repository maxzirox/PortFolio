

services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8001:80"  # Mapea el puerto 8001 de tu máquina al puerto 80 del contenedor
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge
