services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"  # Cambiado de 8001:80 a 8080:8080
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge