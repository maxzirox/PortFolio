services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8001:80"
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge
