
services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"  # Asegúrate de que el puerto sea el correcto
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge
