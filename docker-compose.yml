

services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf  # Archivo de configuración de Nginx
      - ./build:/usr/share/nginx/html  # Archivos de tu portafolio
    networks:
      - webnet

networks:
  webnet:
